<Window x:Class="VAL.DiagnosticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VAL Diagnostics"
        Height="620"
        Width="860"
        MinHeight="420"
        MinWidth="560"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner"
        Style="{DynamicResource VALWindowTheme}">
    <Border Style="{StaticResource VAL.ToolWindowOuterBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="Diagnostics" Style="{StaticResource VAL.ToolWindowHeaderText}" />

            <Separator Grid.Row="1" />

            <TextBox Grid.Row="2"
                     Text="{Binding DiagnosticsText, Mode=OneWay}"
                     Style="{StaticResource VAL.ToolWindowReportViewer}" />

            <StackPanel Grid.Row="3" Margin="0,16,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
                <WrapPanel>
                    <Button Content="Copy Diagnostics"
                            Width="140"
                            Margin="0,0,8,0"
                            Style="{StaticResource VAL.ToolWindowPrimaryButton}"
                            Command="{Binding CopyDiagnosticsCommand}" />
                    <Button Content="Open Logs"
                            Width="110"
                            Margin="0,0,8,0"
                            Style="{StaticResource VAL.ToolWindowSecondaryButton}"
                            Command="{Binding OpenLogsFolderCommand}" />
                    <Button Content="Open Data"
                            Width="110"
                            Margin="0,0,8,0"
                            Style="{StaticResource VAL.ToolWindowSecondaryButton}"
                            Command="{Binding OpenDataFolderCommand}" />
                    <Button Content="Open Modules"
                            Width="120"
                            Margin="0,0,8,0"
                            Style="{StaticResource VAL.ToolWindowSecondaryButton}"
                            Command="{Binding OpenModulesFolderCommand}" />
                    <Button Content="Open Profile"
                            Width="110"
                            Style="{StaticResource VAL.ToolWindowSecondaryButton}"
                            Command="{Binding OpenProfileFolderCommand}" />
                </WrapPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
