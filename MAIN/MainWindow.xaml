<Window x:Class="VAL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="VAL" Height="720" Width="1280"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        Style="{DynamicResource VALWindowTheme}">
    <Window.InputBindings>
        <KeyBinding Key="D"
                    Modifiers="Control+Alt"
                    Command="{Binding OpenDiagnosticsCommand}" />
    </Window.InputBindings>
    <Grid>
        <wv2:WebView2 x:Name="WebView" />

        <Button x:Name="ControlCentreButton"
                Panel.ZIndex="9999"
                Width="30"
                Height="30"
                Margin="0,8,12,0"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                ToolTip="Control Centre"
                Click="ControlCentreButton_Click"
                AutomationProperties.Name="Control Centre"
                Cursor="Hand"
                BorderThickness="1"
                BorderBrush="#4A74A4D2"
                Background="#4025446C"
                Foreground="#E8F4FF"
                FocusVisualStyle="{x:Null}">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="LensBorder"
                                        CornerRadius="15"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        Background="{TemplateBinding Background}">
                                    <Grid>
                                        <Image x:Name="ControlCentreButtonImage"
                                               Width="18"
                                               Height="18"
                                               Stretch="Uniform"
                                               SnapsToDevicePixels="True"
                                               RenderOptions.BitmapScalingMode="HighQuality" />
                                        <TextBlock x:Name="ControlCentreFallbackText"
                                                   Text="CC"
                                                   FontSize="11"
                                                   FontWeight="SemiBold"
                                                   Visibility="Collapsed"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center" />
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="LensBorder" Property="BorderBrush" Value="#88A5D4FF" />
                                        <Setter TargetName="LensBorder" Property="Background" Value="#5A315D8F" />
                                        <Setter TargetName="LensBorder" Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect BlurRadius="14"
                                                                  ShadowDepth="0"
                                                                  Color="#6A4AB8FF"
                                                                  Opacity="0.9" />
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                    <Trigger Property="IsKeyboardFocused" Value="True">
                                        <Setter TargetName="LensBorder" Property="BorderBrush" Value="#99B8DCFF" />
                                        <Setter TargetName="LensBorder" Property="Background" Value="#5A315D8F" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>
