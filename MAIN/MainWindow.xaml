<Window x:Class="VAL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="VAL" Height="720" Width="1280"
        WindowStartupLocation="CenterScreen"
        Background="#0B0C10" WindowStyle="SingleBorderWindow">
    <Window.InputBindings>
        <KeyBinding Key="D"
                    Modifiers="Control+Alt"
                    Command="{Binding OpenDiagnosticsCommand}" />
    </Window.InputBindings>
    <Grid Background="#0B0C10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Menu Grid.Row="0"
              Background="#0B0C10"
              Foreground="#E6EAF7"
              BorderBrush="#1B2330">
            <MenuItem Header="_Tools">
                <MenuItem Header="Truth Health"
                          Command="{Binding OpenTruthHealthCommand}" />
                <Separator />
                <MenuItem Header="Diagnostics"
                          Command="{Binding OpenDiagnosticsCommand}" />
            </MenuItem>
        </Menu>

        <wv2:WebView2 x:Name="WebView"
                      Grid.Row="1" />
    </Grid>
</Window>
