<Window x:Class="VAL.ControlCentreOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Width="30"
        Height="30"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="False"
        SizeToContent="Manual"
        ShowActivated="False">
    <Grid x:Name="LayoutRoot"
          Width="30"
          Height="30">
        <Border x:Name="LayoutOutline"
                BorderThickness="1"
                BorderBrush="Transparent"
                CornerRadius="15" />
        <Button x:Name="LauncherButton"
                Width="30"
                Height="30"
                ToolTip="Control Centre"
                AutomationProperties.Name="Control Centre"
                Cursor="Hand"
                BorderThickness="1"
                BorderBrush="#2A4A66"
                Background="#151F2F"
                Foreground="#E8F4FF"
                Padding="0"
                Click="LauncherButton_Click"
                PreviewMouseRightButtonDown="LauncherButton_PreviewMouseRightButtonDown"
                PreviewMouseMove="LauncherButton_PreviewMouseMove"
                PreviewMouseRightButtonUp="LauncherButton_PreviewMouseRightButtonUp"
                FocusVisualStyle="{x:Null}">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="LauncherBorder"
                                        CornerRadius="15"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        Background="{TemplateBinding Background}">
                                    <Grid>
                                        <Image x:Name="LauncherImage"
                                               Width="18"
                                               Height="18"
                                               Stretch="Uniform"
                                               Visibility="Collapsed"
                                               RenderOptions.BitmapScalingMode="HighQuality" />
                                        <TextBlock x:Name="LauncherFallbackText"
                                                   Text="CC"
                                                   FontSize="11"
                                                   FontWeight="SemiBold"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Visibility="Visible" />
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="LauncherBorder" Property="BorderBrush" Value="#5D7AA0" />
                                        <Setter TargetName="LauncherBorder" Property="Background" Value="#1F2A3F" />
                                        <Setter TargetName="LauncherBorder" Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect BlurRadius="14"
                                                                  ShadowDepth="0"
                                                                  Color="#4A8FD0"
                                                                  Opacity="0.45" />
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="LauncherBorder" Property="Background" Value="#111B2A" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
        <Thumb x:Name="ResizeThumb"
               Width="10"
               Height="10"
               HorizontalAlignment="Right"
               VerticalAlignment="Bottom"
               Margin="0"
               Cursor="SizeNWSE"
               Visibility="Collapsed"
               DragDelta="ResizeThumb_DragDelta"
               DragCompleted="ResizeThumb_DragCompleted">
            <Thumb.Template>
                <ControlTemplate TargetType="Thumb">
                    <Border Background="#CC4A8FD0"
                            BorderBrush="#EE8CCBFF"
                            BorderThickness="1"
                            CornerRadius="3" />
                </ControlTemplate>
            </Thumb.Template>
        </Thumb>
    </Grid>
</Window>
